# Commit and PR conventions
- Never include `Claude-Session:` lines or claude.ai session links in commit messages, PR titles or PR descriptions.
- Commit messages and PR titles follow Conventional Commits (they drive semantic-release).
- Never write the literal string `[skip ci]` in a commit message or PR text; only the automated release commit uses it.
